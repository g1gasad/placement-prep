#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
    bool doesAliceWin(string s) {
        int n=s.size();
        alice_can_play=true;
        bob_can_play=true;
        int i=0, j=0;
        while(alice_can_play){
            for(i; i<n; i++){
                if(s[i]=='a' || )
            }
        }
    }
};
int main() {
    Solution sol;
    string s="leetcoder";
    bool ans=sol.doesAliceWin(s);
    if(ans) cout << "Alicia";
    else << "Bobia";
    return 0;
}